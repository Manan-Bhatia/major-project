import { Metadata } from "next";
export const metadata: Metadata = {
    title: "Teacher",
};

export default function Teacher() {
    return (
        <>
            <div>
                <h1>Teacher</h1>
            </div>
        </>
    );
}
